# pnpm v11: .npmrc is ONLY for auth and registry settings.
# All other settings (hoisting, linker, save-exact, etc.) live in pnpm-workspace.yaml.
registry=https://registry.npmjs.org/
@acontplus:registry=https://registry.npmjs.org/
access=public
