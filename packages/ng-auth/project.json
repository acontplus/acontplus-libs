{
  "name": "ng-auth",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/ng-auth/src",
  "prefix": "acp",
  "projectType": "library",
  "release": {
    "version": {
      "manifestRootsToUpdate": ["dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": ["npm:public", "npm:acontplus", "npm:angular", "npm:authentication"],
  "implicitDependencies": ["ng-infrastructure", "ng-config"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "defaultConfiguration": "production",
      "options": {
        "project": "packages/ng-auth/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "packages/ng-auth/tsconfig.lib.prod.json"
        },
        "development": {}
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm publish --access=public",
        "cwd": "dist/packages/ng-auth",
        "parallel": false
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    }
  }
}
